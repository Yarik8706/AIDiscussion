# AGENTS.md

## Project: Python Application

### Purpose
Этот проект — Python-приложение. Codex помогает с разработкой, тестированием, документацией и рефакторингом.

---

## Setup

Перед началом задач Codex должен убедиться в наличии зависимостей:

```bash
python -m venv .venv
source .venv/bin/activate  # или .venv\Scripts\activate в Windows
pip install -r requirements.txt
